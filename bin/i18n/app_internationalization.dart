// GENERATED CODE - DO NOT MODIFY BY HAND
import 'dart:convert';
import 'package:blogger_theme/blogger_theme.dart';
import 'i18n_config.dart';

/// A custom localized text component supporting runtime variables and translation.
class I18nText extends Component {
  final String key;
  final String fallback;
  final Map<String, dynamic>? variables;

  const I18nText(this.key, {required this.fallback, this.variables});

  @override
  Iterable<Component> build() {
    return [
      DomComponent(
        'span',
        attributes: {
          'data-i18n': key,
          if (variables != null) 'data-i18n-vars': json.encode(variables),
        },
        children: [Text(fallback)],
      )
    ];
  }
}

/// Flutter-style autogenerated internationalization class for type-safe theme development.
class AppI18n {
  final String locale;

  AppI18n(this.locale);

  static AppI18n get current => AppI18n(I18nConfig.defaultLocale);

  static final Map<String, Map<String, String>> _localizedValues = Map<String, Map<String, String>>.from(
    json.decode(r'''{"en":{"session_settings":"Session Settings","preferences":"Preferences","language_locale":"Language & Locale","preferred_currency":"Preferred Currency","account_uid":"Account USER ID","logout_session":"Logout Session","shopping_bag":"Shopping Bag","total":"Total","confirm_order":"Confirm Order","select_location":"Select Location","detect_location":"Detect My Location","or":"OR","apply":"Apply","find":"Find","search_placeholder":"Service title, keywords, or company","location_placeholder":"City, PIN code","guest_user":"Guest User","workspace_client":"Workspace Client"},"fr":{"session_settings":"Paramètres de session","preferences":"Préférences","language_locale":"Langue & Paramètres régionaux","preferred_currency":"Devise préférée","account_uid":"ID utilisateur du compte","logout_session":"Fermer la session","shopping_bag":"Sac de courses","total":"Total","confirm_order":"Confirmer la commande","select_location":"Sélectionnez l'emplacement","detect_location":"Détecter ma position","or":"OU","apply":"Appliquer","find":"Trouver","search_placeholder":"Titre du service, mots-clés ou entreprise","location_placeholder":"Ville, code PIN","guest_user":"Utilisateur invité","workspace_client":"Client de l'espace de travail"},"hi":{"session_settings":"सत्र सेटिंग्स","preferences":"प्राथमिकताएं","language_locale":"भाषा और स्थानीयकरण","preferred_currency":"पसंदीदा मुद्रा","account_uid":"खाता उपयोगकर्ता आईडी","logout_session":"सत्र समाप्त करें","shopping_bag":"शॉपिंग बैग","total":"कुल","confirm_order":"ऑर्डर की पुष्टि करें","select_location":"स्थान चुनें","detect_location":"मेरा स्थान पता करें","or":"या","apply":"लागू करें","find":"खोजें","search_placeholder":"सेवा शीर्षक, कीवर्ड या कंपनी","location_placeholder":"शहर, पिन कोड","guest_user":"अतिथि उपयोगकर्ता","workspace_client":"कार्यक्षेत्र क्लाइंट"}}''').map(
      (k, v) => MapEntry(k, Map<String, String>.from(v))
    )
  );

  /// Standard translation lookup method
  String translate(String key, [Map<String, dynamic>? variables]) {
    final dict = _localizedValues[locale] ?? _localizedValues[I18nConfig.fallbackLocale] ?? {};
    final message = dict[key] ?? _localizedValues[I18nConfig.fallbackLocale]?[key] ?? key;
    if (variables == null || variables.isEmpty) {
      return message;
    }
    var result = message;
    variables.forEach((k, v) {
      result = result.replaceAll('{$k}', v.toString());
    });
    return result;
  }

  // =========================================================================
  // Key: "session_settings"
  // =========================================================================

  /// Returns localized String fallback: "Session Settings"
  String get session_settings => translate('session_settings');

  /// Returns localized self-translating Component fallback: "Session Settings"
  Component get session_settingsComponent => I18nText('session_settings', fallback: translate('session_settings'));

  // =========================================================================
  // Key: "preferences"
  // =========================================================================

  /// Returns localized String fallback: "Preferences"
  String get preferences => translate('preferences');

  /// Returns localized self-translating Component fallback: "Preferences"
  Component get preferencesComponent => I18nText('preferences', fallback: translate('preferences'));

  // =========================================================================
  // Key: "language_locale"
  // =========================================================================

  /// Returns localized String fallback: "Language & Locale"
  String get language_locale => translate('language_locale');

  /// Returns localized self-translating Component fallback: "Language & Locale"
  Component get language_localeComponent => I18nText('language_locale', fallback: translate('language_locale'));

  // =========================================================================
  // Key: "preferred_currency"
  // =========================================================================

  /// Returns localized String fallback: "Preferred Currency"
  String get preferred_currency => translate('preferred_currency');

  /// Returns localized self-translating Component fallback: "Preferred Currency"
  Component get preferred_currencyComponent => I18nText('preferred_currency', fallback: translate('preferred_currency'));

  // =========================================================================
  // Key: "account_uid"
  // =========================================================================

  /// Returns localized String fallback: "Account USER ID"
  String get account_uid => translate('account_uid');

  /// Returns localized self-translating Component fallback: "Account USER ID"
  Component get account_uidComponent => I18nText('account_uid', fallback: translate('account_uid'));

  // =========================================================================
  // Key: "logout_session"
  // =========================================================================

  /// Returns localized String fallback: "Logout Session"
  String get logout_session => translate('logout_session');

  /// Returns localized self-translating Component fallback: "Logout Session"
  Component get logout_sessionComponent => I18nText('logout_session', fallback: translate('logout_session'));

  // =========================================================================
  // Key: "shopping_bag"
  // =========================================================================

  /// Returns localized String fallback: "Shopping Bag"
  String get shopping_bag => translate('shopping_bag');

  /// Returns localized self-translating Component fallback: "Shopping Bag"
  Component get shopping_bagComponent => I18nText('shopping_bag', fallback: translate('shopping_bag'));

  // =========================================================================
  // Key: "total"
  // =========================================================================

  /// Returns localized String fallback: "Total"
  String get total => translate('total');

  /// Returns localized self-translating Component fallback: "Total"
  Component get totalComponent => I18nText('total', fallback: translate('total'));

  // =========================================================================
  // Key: "confirm_order"
  // =========================================================================

  /// Returns localized String fallback: "Confirm Order"
  String get confirm_order => translate('confirm_order');

  /// Returns localized self-translating Component fallback: "Confirm Order"
  Component get confirm_orderComponent => I18nText('confirm_order', fallback: translate('confirm_order'));

  // =========================================================================
  // Key: "select_location"
  // =========================================================================

  /// Returns localized String fallback: "Select Location"
  String get select_location => translate('select_location');

  /// Returns localized self-translating Component fallback: "Select Location"
  Component get select_locationComponent => I18nText('select_location', fallback: translate('select_location'));

  // =========================================================================
  // Key: "detect_location"
  // =========================================================================

  /// Returns localized String fallback: "Detect My Location"
  String get detect_location => translate('detect_location');

  /// Returns localized self-translating Component fallback: "Detect My Location"
  Component get detect_locationComponent => I18nText('detect_location', fallback: translate('detect_location'));

  // =========================================================================
  // Key: "or"
  // =========================================================================

  /// Returns localized String fallback: "OR"
  String get or => translate('or');

  /// Returns localized self-translating Component fallback: "OR"
  Component get orComponent => I18nText('or', fallback: translate('or'));

  // =========================================================================
  // Key: "apply"
  // =========================================================================

  /// Returns localized String fallback: "Apply"
  String get apply => translate('apply');

  /// Returns localized self-translating Component fallback: "Apply"
  Component get applyComponent => I18nText('apply', fallback: translate('apply'));

  // =========================================================================
  // Key: "find"
  // =========================================================================

  /// Returns localized String fallback: "Find"
  String get find => translate('find');

  /// Returns localized self-translating Component fallback: "Find"
  Component get findComponent => I18nText('find', fallback: translate('find'));

  // =========================================================================
  // Key: "search_placeholder"
  // =========================================================================

  /// Returns localized String fallback: "Service title, keywords, or company"
  String get search_placeholder => translate('search_placeholder');

  /// Returns localized self-translating Component fallback: "Service title, keywords, or company"
  Component get search_placeholderComponent => I18nText('search_placeholder', fallback: translate('search_placeholder'));

  // =========================================================================
  // Key: "location_placeholder"
  // =========================================================================

  /// Returns localized String fallback: "City, PIN code"
  String get location_placeholder => translate('location_placeholder');

  /// Returns localized self-translating Component fallback: "City, PIN code"
  Component get location_placeholderComponent => I18nText('location_placeholder', fallback: translate('location_placeholder'));

  // =========================================================================
  // Key: "guest_user"
  // =========================================================================

  /// Returns localized String fallback: "Guest User"
  String get guest_user => translate('guest_user');

  /// Returns localized self-translating Component fallback: "Guest User"
  Component get guest_userComponent => I18nText('guest_user', fallback: translate('guest_user'));

  // =========================================================================
  // Key: "workspace_client"
  // =========================================================================

  /// Returns localized String fallback: "Workspace Client"
  String get workspace_client => translate('workspace_client');

  /// Returns localized self-translating Component fallback: "Workspace Client"
  Component get workspace_clientComponent => I18nText('workspace_client', fallback: translate('workspace_client'));

}
